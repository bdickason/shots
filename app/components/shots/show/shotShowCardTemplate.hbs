<a href="/{{ projectId }}/{{ id }}" class="shotlink" id="{{ projectId }}/{{ id }}">Posted {{ time }} by {{ user }}</a><br />
<img src="{{ image }}" class="shotImage">
<p id="shotText">{{ text }}</p>
<div class="shotCreator">
    {{! UserCard loads here }}
</div>
<a href="/{{ projectId }}/{{ id }}">
    <div class="shotComments">
        {{! CommentsListCard loads here }}
    </div>
</a>